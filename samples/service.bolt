
path / {
  read() { false }
  write() { true }
}

service flubber {
  path / {
    read() { true }
    write() { true }
  }
}

service gcs.googleapis.com {
  path / {
    read() { true }
    write() { false }
  }
}
