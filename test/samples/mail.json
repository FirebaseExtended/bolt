{
  "rules": {
    "users": {
      "$ownerid": {
        ".read": "auth.username == $ownerid",
        "inbox": {
          "$msg": {
            ".write": "data.val() == null && auth != null || newData.val() == null && auth.username == $owerid",
            ".validate": "newData.child('from').isString() && newData.child('to').isString() && newData.child('message').isString() && auth.username == newData.child('from').val() && newData.child('to').val() == $ownerid"
          }
        },
        "outbox": {
          "$msg": {
            ".write": "auth.username == $ownerid && (data.val() == null || newData.val() == null)",
            ".validate": "newData.child('from').isString() && newData.child('to').isString() && newData.child('message').isString() && auth.username == newData.child('from').val()"
          }
        }
      }
    }
  }
}
