{
  "rules": {
    "parents": {
      "$parent": {
        "$child": {
          ".validate": "newData.hasChildren(['age'])",
          "age": {
            ".validate": "newData.isNumber()"
          },
          "$other": {
            ".validate": "false"
          }
        }
      }
    }
  }
}
